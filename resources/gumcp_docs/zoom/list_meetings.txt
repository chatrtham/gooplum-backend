Parameters Schema:
```json
{
  "type": {
    "type": "string",
    "description": "Meeting type: scheduled (all valid previous, live, upcoming), live (ongoing), upcoming (all upcoming including live), upcoming_meetings (all upcoming including live), previous_meetings (all previous)",
    "enum": [
      "scheduled",
      "live",
      "upcoming",
      "upcoming_meetings",
      "previous_meetings"
    ],
    "default": "scheduled"
  },
  "max_limit": {
    "type": "integer",
    "description": "Maximum number of meetings to return across all pages (automatically handles pagination)",
    "default": 100
  },
  "from": {
    "type": "string",
    "description": "Start date (YYYY-MM-DD format)"
  },
  "to": {
    "type": "string",
    "description": "End date (YYYY-MM-DD format)"
  },
  "timezone": {
    "type": "string",
    "description": "Timezone to assign to the from and to values"
  }
}
```
