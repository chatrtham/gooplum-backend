Parameters Schema:
```json
{
  "max_limit": {
    "type": "integer",
    "description": "Maximum number of recordings to return across all pages (automatically handles pagination)",
    "default": 100
  },
  "from": {
    "type": "string",
    "description": "Start date in YYYY-MM-DD UTC format for date range. If not provided, defaults to current date."
  },
  "to": {
    "type": "string",
    "description": "End date in YYYY-MM-DD UTC format. Maximum range is one month."
  },
  "trash": {
    "type": "boolean",
    "description": "Query trash recordings or active recordings",
    "default": false
  },
  "trash_type": {
    "type": "string",
    "description": "Type of recording to retrieve from trash",
    "enum": [
      "meeting_recordings",
      "recording_file"
    ],
    "default": "meeting_recordings"
  },
  "meeting_id": {
    "type": "integer",
    "description": "Filter recordings by specific meeting ID"
  },
  "mc": {
    "type": "string",
    "description": "Query metadata of recording if using on-premise meeting connector",
    "default": "false"
  }
}
```
