Parameters Schema:
```json
{
  "max_limit": {
    "type": "integer",
    "description": "Maximum number of audit reports to return (will handle pagination automatically)",
    "default": 10
  },
  "sort_by": {
    "type": "string",
    "description": "Field to sort by",
    "enum": [
      "inserted_at"
    ]
  },
  "sort_direction": {
    "type": "string",
    "description": "Sort direction",
    "enum": [
      "ASC",
      "DESC"
    ]
  }
}
```
