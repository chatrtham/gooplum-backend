Parameters Schema:
```json
{
  "post_id": {
    "type": "string",
    "description": "Zendesk post ID to retrieve (required)"
  },
  "include": {
    "type": "array",
    "items": {
      "type": "string",
      "enum": [
        "users",
        "topics"
      ]
    },
    "description": "Additional data to include: users (authors), topics"
  }
}
```
