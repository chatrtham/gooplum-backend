Parameters Schema:
```json
{
  "max_limit": {
    "type": "integer",
    "description": "Maximum number of users to return across all pages",
    "default": 100
  },
  "orderBy": {
    "type": "string",
    "enum": [
      "createdAt",
      "updatedAt"
    ],
    "description": "Order by field",
    "default": "updatedAt"
  },
  "query": {
    "type": "string",
    "description": "Optional query to filter users by name or email"
  },
  "includeDisabled": {
    "type": "boolean",
    "description": "Whether to include disabled users",
    "default": false
  }
}
```
