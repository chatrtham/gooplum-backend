Parameters Schema:
```json
{
  "filter": {
    "type": "string",
    "description": "Filter expression (e.g., 'name:eq:Sales', 'projectName:eq:Finance'). Supports fields: name, contentUrl, createdAt, updatedAt, ownerName, projectName, tags, type. Operators: eq, in, gt, gte, lt, lte."
  },
  "max_limit": {
    "type": "integer",
    "description": "Maximum number of datasources to return across all pages"
  }
}
```
