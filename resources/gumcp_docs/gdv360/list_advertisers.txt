Parameters Schema:
```json
{
  "partner_id": {
    "type": "string",
    "description": "Required. The ID of the partner that the fetched advertisers should all belong to (int64 format as string)"
  },
  "filter": {
    "type": "string",
    "description": "Filter expression for advertisers (e.g., 'entityStatus=\"ENTITY_STATUS_ACTIVE\"')"
  },
  "order_by": {
    "type": "string",
    "description": "Field to sort by: advertiserId (default), displayName, entityStatus, updateTime. Add 'desc' for descending (e.g., 'displayName desc')"
  },
  "max_limit": {
    "type": "integer",
    "description": "Maximum number of advertisers to return across all pages (automatically handles pagination)",
    "default": 100
  }
}
```
