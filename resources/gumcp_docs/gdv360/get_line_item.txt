Parameters Schema:
```json
{
  "advertiser_id": {
    "type": "string",
    "description": "Required. The ID of the advertiser this line item belongs to (int64 format as string)"
  },
  "line_item_id": {
    "type": "string",
    "description": "Required. The ID of the line item to fetch (int64 format as string)"
  }
}
```
