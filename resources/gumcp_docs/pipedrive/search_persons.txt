Parameters Schema:
```json
{
  "term": {
    "type": "string",
    "description": "The search term to look for. Minimum 2 characters (or 1 if using exact_match). URL encoded."
  },
  "fields": {
    "type": "string",
    "description": "A comma-separated string array. The fields to perform the search from. Values: custom_fields, email, notes, phone, name. Defaults to all."
  },
  "exact_match": {
    "type": "boolean",
    "description": "When enabled, only full exact matches against the given term are returned. Not case sensitive.",
    "default": false
  },
  "organization_id": {
    "type": "integer",
    "description": "Will filter persons by the provided organization ID. The upper limit of found persons associated with the organization is 2000."
  },
  "include_fields": {
    "type": "string",
    "description": "Supports including optional fields in the results. Values: person.picture"
  },
  "max_limit": {
    "type": "integer",
    "description": "Maximum number of persons to return across all pages (automatically handles pagination). Default: 100",
    "default": 100
  }
}
```
