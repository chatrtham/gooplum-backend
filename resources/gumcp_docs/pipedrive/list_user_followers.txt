Parameters Schema:
```json
{
  "user_id": {
    "type": "integer",
    "description": "The ID of the user"
  },
  "max_limit": {
    "type": "integer",
    "description": "Maximum number of followers to return across all pages (automatically handles pagination). Default: 100",
    "default": 100
  }
}
```
