Parameters Schema:
```json
{
  "repo_url": {
    "type": "string",
    "description": "GitHub repository URL (preferred over owner/repo_name)"
  },
  "owner": {
    "type": "string",
    "description": "Repository owner username. Use repo_url instead if available."
  },
  "repo_name": {
    "type": "string",
    "description": "Repository name. Use repo_url instead if available."
  },
  "max_limit": {
    "type": "integer",
    "description": "Maximum number of tags to return across all pages"
  },
  "query": {
    "type": "string",
    "description": "Filters tags with query on tag name"
  },
  "orderBy_field": {
    "type": "string",
    "enum": [
      "TAG_COMMIT_DATE",
      "ALPHABETICAL"
    ],
    "description": "The field to order tags by"
  },
  "orderBy_direction": {
    "type": "string",
    "enum": [
      "ASC",
      "DESC"
    ],
    "description": "The ordering direction"
  }
}
```
