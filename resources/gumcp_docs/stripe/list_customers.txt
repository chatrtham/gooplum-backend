Parameters Schema:
```json
{
  "email": {
    "type": "string",
    "description": "A case-sensitive filter on the list based on the customer's email field. The value must be a string"
  },
  "created": {
    "type": "object",
    "properties": {
      "gt": {
        "type": "integer",
        "description": "Return customers created after this timestamp (exclusive)"
      },
      "gte": {
        "type": "integer",
        "description": "Return customers created after or equal to this timestamp (inclusive)"
      },
      "lt": {
        "type": "integer",
        "description": "Return customers created before this timestamp (exclusive)"
      },
      "lte": {
        "type": "integer",
        "description": "Return customers created before or equal to this timestamp (inclusive)"
      }
    },
    "description": "Only return customers that were created during the given date interval using Unix timestamps"
  },
  "ending_before": {
    "type": "string",
    "description": "A cursor for use in pagination. ending_before is an object ID that defines your place in the list"
  },
  "limit": {
    "type": "integer",
    "description": "A limit on the number of objects to be returned. Limit can range between 1 and 100, and the default is 10"
  },
  "starting_after": {
    "type": "string",
    "description": "A cursor for use in pagination. starting_after is an object ID that defines your place in the list"
  },
  "test_clock": {
    "type": "string",
    "description": "Provides a list of customers that are associated with the specified test clock. The response will not include customers with test clocks if this parameter is not set"
  }
}
```
