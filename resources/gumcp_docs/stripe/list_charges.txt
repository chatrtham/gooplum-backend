Parameters Schema:
```json
{
  "customer": {
    "type": "string",
    "description": "Only return charges for the customer specified by this customer ID"
  },
  "customer_email": {
    "type": "string",
    "description": "Only return charges for the customer specified by this email (alternative to customer)"
  },
  "created": {
    "type": "object",
    "properties": {
      "gt": {
        "type": "integer",
        "description": "Return charges created after this timestamp (exclusive)"
      },
      "gte": {
        "type": "integer",
        "description": "Return charges created after or equal to this timestamp (inclusive)"
      },
      "lt": {
        "type": "integer",
        "description": "Return charges created before this timestamp (exclusive)"
      },
      "lte": {
        "type": "integer",
        "description": "Return charges created before or equal to this timestamp (inclusive)"
      }
    },
    "description": "Only return charges that were created during the given date interval using Unix timestamps"
  },
  "ending_before": {
    "type": "string",
    "description": "A cursor for use in pagination. ending_before is an object ID that defines your place in the list"
  },
  "limit": {
    "type": "integer",
    "description": "A limit on the number of objects to be returned. Limit can range between 1 and 100, and the default is 10"
  },
  "payment_intent": {
    "type": "string",
    "description": "Only return charges that were created by the PaymentIntent specified by this PaymentIntent ID"
  },
  "starting_after": {
    "type": "string",
    "description": "A cursor for use in pagination. starting_after is an object ID that defines your place in the list"
  },
  "transfer_group": {
    "type": "string",
    "description": "Only return charges for this transfer group (Connect only, limited to 100)"
  }
}
```
