Parameters Schema:
```json
{
  "task_gid": {
    "type": "string",
    "description": "GID of the task to get dependencies for (required)"
  },
  "max_limit": {
    "type": "integer",
    "description": "Maximum number of dependencies to return across all pages",
    "default": 100,
    "maximum": 1000
  },
  "opt_fields": {
    "type": "array",
    "items": {
      "type": "string"
    },
    "description": "Optional fields to include in response"
  }
}
```
