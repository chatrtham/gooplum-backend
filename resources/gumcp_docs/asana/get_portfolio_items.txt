Parameters Schema:
```json
{
  "portfolio_gid": {
    "type": "string",
    "description": "GID of the portfolio to get items from (required)"
  },
  "max_limit": {
    "type": "integer",
    "description": "Maximum number of items to return across all pages",
    "default": 100,
    "maximum": 1000
  },
  "opt_fields": {
    "type": "array",
    "items": {
      "type": "string"
    },
    "description": "Optional fields to include in response"
  }
}
```
