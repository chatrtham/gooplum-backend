Parameters Schema:
```json
{
  "workspace": {
    "type": "string",
    "description": "Workspace GID (required)"
  },
  "start_on": {
    "type": "string",
    "description": "ISO 8601 date string (YYYY-MM-DD) for filtering time periods that start on or after this date"
  },
  "end_on": {
    "type": "string",
    "description": "ISO 8601 date string (YYYY-MM-DD) for filtering time periods that end on or before this date"
  },
  "opt_fields": {
    "type": "array",
    "items": {
      "type": "string"
    },
    "description": "Optional fields to include in response (e.g., parent)"
  }
}
```
